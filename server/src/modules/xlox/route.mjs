// import { Router } from "express";

export const epicSechma =
  "Generate documentation of the code block in JSDoc format for ";

// Router.post("/", (req, res) => {
//   const { body } = req;
//   const msg = `Generate ${body}`;

// });
